WMDB:
  Forger:
    Boards:
      SS_6214:
        Name: 6.2.14 Stabilisation
        Query:
          bool:
            must:
              - term: {focus.name: Stabilization Sprint}
              - term: {fixed_version.name: 6.2.14}
      SP_74:
        Name: 7.4 Stabilisation
        Query:
          bool:
            must:
              - term: {focus.name: Stabilization Sprint}
              - term: {fixed_version.name: 7.4 (Backend)}
      ParisSprint:
        Name: Paris Sprint
        Query:
          bool:
            must:
              - term: {focus.name: On Location Sprint}
              - term: {fixed_version.name: 7.4 (Backend)}
            must_not:
              - term: {subject: wip}
      RS_74:
        Name: 7.4 Board
        Query:
          bool:
            must:
              - term: {focus.name: Remote Sprint}
              - term: {fixed_version.name: 7.4 (Backend)}
            must_not:
              - term: {subject: wip}